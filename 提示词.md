### 1. 特写镜头组 (Close-up)
| 英文提示词 (English) | 中文翻译 (Chinese) |
| :--- | :--- |
| `<sks>` front view low-angle shot close-up | `<sks>` 正面低仰角特写 |
| `<sks>` front-right quarter view low-angle shot close-up | `<sks>` 右前方低仰角特写 |
| `<sks>` right side view low-angle shot close-up | `<sks>` 右侧面低仰角特写 |
| `<sks>` back-right quarter view low-angle shot close-up | `<sks>` 右后方低仰角特写 |
| `<sks>` back view low-angle shot close-up | `<sks>` 背面低仰角特写 |
| `<sks>` back-left quarter view low-angle shot close-up | `<sks>` 左后方低仰角特写 |
| `<sks>` left side view low-angle shot close-up | `<sks>` 左侧面低仰角特写 |
| `<sks>` front-left quarter view low-angle shot close-up | `<sks>` 左前方低仰角特写 |
| `<sks>` front view eye-level shot close-up | `<sks>` 正面平视特写 |
| `<sks>` front-right quarter view eye-level shot close-up | `<sks>` 右前方平视特写 |
| `<sks>` right side view eye-level shot close-up | `<sks>` 右侧面平视特写 |
| `<sks>` back-right quarter view eye-level shot close-up | `<sks>` 右后方平视特写 |
| `<sks>` back view eye-level shot close-up | `<sks>` 背面平视特写 |
| `<sks>` back-left quarter view eye-level shot close-up | `<sks>` 左后方平视特写 |
| `<sks>` left side view eye-level shot close-up | `<sks>` 左侧面平视特写 |
| `<sks>` front-left quarter view eye-level shot close-up | `<sks>` 左前方平视特写 |
| `<sks>` front view elevated shot close-up | `<sks>` 正面高位特写 |
| `<sks>` front-right quarter view elevated shot close-up | `<sks>` 右前方高位特写 |
| `<sks>` right side view elevated shot close-up | `<sks>` 右侧面高位特写 |
| `<sks>` back-right quarter view elevated shot close-up | `<sks>` 右后方高位特写 |
| `<sks>` back view elevated shot close-up | `<sks>` 背面高位特写 |
| `<sks>` back-left quarter view elevated shot close-up | `<sks>` 左后方高位特写 |
| `<sks>` left side view elevated shot close-up | `<sks>` 左侧面高位特写 |
| `<sks>` front-left quarter view elevated shot close-up | `<sks>` 左前方高位特写 |
| `<sks>` front view high-angle shot close-up | `<sks>` 正面高角度俯拍特写 |
| `<sks>` front-right quarter view high-angle shot close-up | `<sks>` 右前方高角度俯拍特写 |
| `<sks>` right side view high-angle shot close-up | `<sks>` 右侧面高角度俯拍特写 |
| `<sks>` back-right quarter view high-angle shot close-up | `<sks>` 右后方高角度俯拍特写 |
| `<sks>` back view high-angle shot close-up | `<sks>` 背面高角度俯拍特写 |
| `<sks>` back-left quarter view high-angle shot close-up | `<sks>` 左后方高角度俯拍特写 |
| `<sks>` left side view high-angle shot close-up | `<sks>` 左侧面高角度俯拍特写 |
| `<sks>` front-left quarter view high-angle shot close-up | `<sks>` 左前方高角度俯拍特写 |

### 2. 中景镜头组 (Medium Shot)
| 英文提示词 (English) | 中文翻译 (Chinese) |
| :--- | :--- |
| `<sks>` front view low-angle shot medium shot | `<sks>` 正面低仰角中景 |
| `<sks>` front-right quarter view low-angle shot medium shot | `<sks>` 右前方低仰角中景 |
| `<sks>` right side view low-angle shot medium shot | `<sks>` 右侧面低仰角中景 |
| `<sks>` back-right quarter view low-angle shot medium shot | `<sks>` 右后方低仰角中景 |
| `<sks>` back view low-angle shot medium shot | `<sks>` 背面低仰角中景 |
| `<sks>` back-left quarter view low-angle shot medium shot | `<sks>` 左后方低仰角中景 |
| `<sks>` left side view low-angle shot medium shot | `<sks>` 左侧面低仰角中景 |
| `<sks>` front-left quarter view low-angle shot medium shot | `<sks>` 左前方低仰角中景 |
| `<sks>` front view eye-level shot medium shot | `<sks>` 正面平视中景 |
| `<sks>` front-right quarter view eye-level shot medium shot | `<sks>` 右前方平视中景 |
| `<sks>` right side view eye-level shot medium shot | `<sks>` 右侧面平视中景 |
| `<sks>` back-right quarter view eye-level shot medium shot | `<sks>` 右后方平视中景 |
| `<sks>` back view eye-level shot medium shot | `<sks>` 背面平视中景 |
| `<sks>` back-left quarter view eye-level shot medium shot | `<sks>` 左后方平视中景 |
| `<sks>` left side view eye-level shot medium shot | `<sks>` 左侧面平视中景 |
| `<sks>` front-left quarter view eye-level shot medium shot | `<sks>` 左前方平视中景 |
| `<sks>` front view elevated shot medium shot | `<sks>` 正面高位中景 |
| `<sks>` front-right quarter view elevated shot medium shot | `<sks>` 右前方高位中景 |
| `<sks>` right side view elevated shot medium shot | `<sks>` 右侧面高位中景 |
| `<sks>` back-right quarter view elevated shot medium shot | `<sks>` 右后方高位中景 |
| `<sks>` back view elevated shot medium shot | `<sks>` 背面高位中景 |
| `<sks>` back-left quarter view elevated shot medium shot | `<sks>` 左后方高位中景 |
| `<sks>` left side view elevated shot medium shot | `<sks>` 左侧面高位中景 |
| `<sks>` front-left quarter view elevated shot medium shot | `<sks>` 左前方高位中景 |
| `<sks>` front view high-angle shot medium shot | `<sks>` 正面高角度俯拍中景 |
| `<sks>` front-right quarter view high-angle shot medium shot | `<sks>` 右前方高角度俯拍中景 |
| `<sks>` right side view high-angle shot medium shot | `<sks>` 右侧面高角度俯拍中景 |
| `<sks>` back-right quarter view high-angle shot medium shot | `<sks>` 右后方高角度俯拍中景 |
| `<sks>` back view high-angle shot medium shot | `<sks>` 背面高角度俯拍中景 |
| `<sks>` back-left quarter view high-angle shot medium shot | `<sks>` 左后方高角度俯拍中景 |
| `<sks>` left side view high-angle shot medium shot | `<sks>` 左侧面高角度俯拍中景 |
| `<sks>` front-left quarter view high-angle shot medium shot | `<sks>` 左前方高角度俯拍中景 |

### 3. 全景镜头组 (Wide Shot)
| 英文提示词 (English) | 中文翻译 (Chinese) |
| :--- | :--- |
| `<sks>` front view low-angle shot wide shot | `<sks>` 正面低仰角全景 |
| `<sks>` front-right quarter view low-angle shot wide shot | `<sks>` 右前方低仰角全景 |
| `<sks>` right side view low-angle shot wide shot | `<sks>` 右侧面低仰角全景 |
| `<sks>` back-right quarter view low-angle shot wide shot | `<sks>` 右后方低仰角全景 |
| `<sks>` back view low-angle shot wide shot | `<sks>` 背面低仰角全景 |
| `<sks>` back-left quarter view low-angle shot wide shot | `<sks>` 左后方低仰角全景 |
| `<sks>` left side view low-angle shot wide shot | `<sks>` 左侧面低仰角全景 |
| `<sks>` front-left quarter view low-angle shot wide shot | `<sks>` 左前方低仰角全景 |
| `<sks>` front view eye-level shot wide shot | `<sks>` 正面平视全景 |
| `<sks>` front-right quarter view eye-level shot wide shot | `<sks>` 右前方平视全景 |
| `<sks>` right side view eye-level shot wide shot | `<sks>` 右侧面平视全景 |
| `<sks>` back-right quarter view eye-level shot wide shot | `<sks>` 右后方平视全景 |
| `<sks>` back view eye-level shot wide shot | `<sks>` 背面平视全景 |
| `<sks>` back-left quarter view eye-level shot wide shot | `<sks>` 左后方平视全景 |
| `<sks>` left side view eye-level shot wide shot | `<sks>` 左侧面平视全景 |
| `<sks>` front-left quarter view eye-level shot wide shot | `<sks>` 左前方平视全景 |
| `<sks>` front view elevated shot wide shot | `<sks>` 正面高位全景 |
| `<sks>` front-right quarter view elevated shot wide shot | `<sks>` 右前方高位全景 |
| `<sks>` right side view elevated shot wide shot | `<sks>` 右侧面高位全景 |
| `<sks>` back-right quarter view elevated shot wide shot | `<sks>` 右后方高位全景 |
| `<sks>` back view elevated shot wide shot | `<sks>` 背面高位全景 |
| `<sks>` back-left quarter view elevated shot wide shot | `<sks>` 左后方高位全景 |
| `<sks>` left side view elevated shot wide shot | `<sks>` 左侧面高位全景 |
| `<sks>` front-left quarter view elevated shot wide shot | `<sks>` 左前方高位全景 |
| `<sks>` front view high-angle shot wide shot | `<sks>` 正面高角度俯拍全景 |
| `<sks>` front-right quarter view high-angle shot wide shot | `<sks>` 右前方高角度俯拍全景 |
| `<sks>` right side view high-angle shot wide shot | `<sks>` 右侧面高角度俯拍全景 |
| `<sks>` back-right quarter view high-angle shot wide shot | `<sks>` 右后方高角度俯拍全景 |
| `<sks>` back view high-angle shot wide shot | `<sks>` 背面高角度俯拍全景 |
| `<sks>` back-left quarter view high-angle shot wide shot | `<sks>` 左后方高角度俯拍全景 |
| `<sks>` left side view high-angle shot wide shot | `<sks>` 左侧面高角度俯拍全景 |
| `<sks>` front-left quarter view high-angle shot wide shot | `<sks>` 左前方高角度俯拍全景 |
